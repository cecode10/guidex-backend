/**
 * Firebase Auth admin operations using the Admin SDK.
 * Requires initializeApp() to have been called (see index.mjs).
 */
export { deleteUserAccount as deleteFirebaseUser } from "./account-deletion-service.mjs";
